if(showFullscreen)
{
	text = window_get_fullscreen() ? "Fullscreen: On" : "Fullscreen: Off";
}

if(showSfx)
{
	text = global.sfx ? "Sound Effects: On" : "Sound Effects: Off";
}

if(showMusic)
{
	text = global.mus ? "Music: On" : "Music: Off";
}